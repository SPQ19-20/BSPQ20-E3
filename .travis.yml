language: python
python:
  - "3.7"

# install dependencies
install:
  - pip install -r requiredlibs.txt

# need mongo first
services:
  - mongodb

# test stuff
script:
  - cd BSPQ20E3
  - python manage.py test

# will only build master branch
branches:
  only:
    - master